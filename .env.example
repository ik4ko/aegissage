# ─────────────────────────────────────────────────────────────────────────────
#  AegisSage — environment
#
#  The site builds and runs with NONE of these set. Everything degrades:
#  contact submissions log to the console instead of persisting, alerts are
#  skipped with a warning, and analytics no-ops. Set them to make it real.
# ─────────────────────────────────────────────────────────────────────────────

# Canonical origin. Drives metadataBase, canonical URLs, sitemap and OG image
# URLs. Set to the Vercel preview URL on previews, the apex domain in prod.
NEXT_PUBLIC_SITE_URL=https://aegissage.com

# ── Advisor identity ─────────────────────────────────────────────────────────
# These override the PLACEHOLDER defaults in lib/site.ts. Set every one of
# them before the site goes live — the defaults are fictional.
NEXT_PUBLIC_ADVISOR_NAME=
NEXT_PUBLIC_ADVISOR_NPN=
NEXT_PUBLIC_ADVISOR_EMAIL=
# Digits only, with country code. Example: 19085550142
NEXT_PUBLIC_ADVISOR_PHONE=
# Path to the headshot under /public, e.g. /advisor.jpg. Falls back to initials.
NEXT_PUBLIC_ADVISOR_PHOTO=

# ── Supabase ─────────────────────────────────────────────────────────────────
# The service role key is server-only and must never be prefixed NEXT_PUBLIC_.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ── Contact alerts: email (Resend) ───────────────────────────────────────────
RESEND_API_KEY=
# Must be a verified sending domain in Resend.
NOTIFY_EMAIL_FROM=AegisSage <notifications@aegissage.com>
# Where new-contact alerts go. Comma-separated for multiple recipients.
NOTIFY_EMAIL_TO=

# ── Contact alerts: SMS (Twilio) ─────────────────────────────────────────────
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
# E.164, e.g. +19085550142
TWILIO_FROM_NUMBER=
NOTIFY_SMS_TO=
